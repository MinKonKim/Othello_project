import React from "react";
import useBoardHistory from "../../stores/useBoardHistory";

// const { undo, redo, updateGrid, resetHistory } = useBoardHistory();

const BoardHistory = () => {
  return (
    <div>
      <ul></ul>
    </div>
  );
};

export default BoardHistory;
